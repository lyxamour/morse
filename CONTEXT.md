# 领域词汇表

## 摩斯转换

把文本与摩斯码互相转换的能力。当前范围包括编码、解码、输入格式自动识别，以及跨平台 App 形态。

## 跨平台 App

同一套核心转换能力服务于桌面端、移动端和 Web。平台外壳可以不同，但转换语义必须一致。

## 电报码

中文字符转换为摩斯码前使用的数字编码中间表示。面向用户的主要输出可以是摩斯码，但系统内部保留电报码中间态，便于回显、排错和反向解析。

## 纯 Morse 输出

用户可见的导出结果以点划摩斯码为主，不要求直接暴露电报码数字。内部仍可保留原文、电报码和规范化结果。

## 规范化输入

转换前对输入做 Unicode 归一化、繁简处理和可识别别名归一；同时保留原文用于 UI 回显和逐字符报表。

## 严格报错

遇到无法转换或无法无歧义解析的字符时，不静默跳过。错误以逐字符报表呈现，指出每个字符的状态、位置和原因。

## 自动识别

解码时尽量识别点划摩斯、英文、数字、中文电报码及常见不规范写法，并归一到统一内部表示。遇到歧义时先按优先级给推荐解释，同时保留候选列表给 UI 展示。

## Morse 线格式

编码输出统一为点划 Morse，字符之间用空格分隔。解码兼容连续点划串和常见不规范分隔写法，但解析结果必须归一到统一格式。

## 固定标准表

中文电报码使用公开可校验的固定标准表。标准表版本随项目固定，避免不同平台或不同时间得到不同 Morse 结果。

## 逐字符报表

转换结果按输入单位返回报表字段：原文、位置、规范化值、电报码中间值、Morse 结果、状态、错误原因、候选解释和调试数据。
